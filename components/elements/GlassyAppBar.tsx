import { AppBar, styled } from "@mui/material";

export const GlassmorphicAppBar = styled(AppBar)`
  background: rgba(255, 255, 255, 0.15);
  backdrop-filter: blur(10px);
  box-shadow: 0 4px 12px 0 rgba(31, 38, 135, 0.05);
`;
